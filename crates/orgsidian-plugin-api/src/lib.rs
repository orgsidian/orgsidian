//! orgsidian-plugin-api: plugin API trait surface (LEAF-of-leaves per LD-26 / LD-10).
//!
//! Structural placeholder — the full `OrgsidianPlugin` trait, `Event` enum,
//! `HookOutcome`, `HookContext` / `PluginContext` traits land in Story 1.5.
