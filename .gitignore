# Folder view configuration files
.DS_Store
Desktop.ini

# Thumbnail cache files
._*
Thumbs.db

# Files that might appear on external disks
.Spotlight-V100
.Trashes

# BMad
_bmad/
.claude/skills/bmad*

# Claude Code local-only
.claude/settings.local.json
.claude/worktrees/

# Environment files
.env
.env.local
.env.*.local

# IDE
.vscode/

# Node / pnpm (root scaffold for commitlint+husky; full frontend wiring lands in Story 1.3)
node_modules/
pnpm-debug.log*

# Vite / Tauri / Cargo workspace build outputs
dist/
dist-ssr/
shell-ui/dist/
target/
crates/orgsidian-shell-app/target/
crates/orgsidian-shell-app/gen/schemas/
tools/corpus-extractor/target/
*.local

# Corpus-extractor download cache (Story 2.5 AC2 — the GPL test-org-element.el
# is pinned-fetched at extraction time, NEVER vendored/committed; R-009/LD-37)
tools/corpus-extractor/cache/

# TanStack Router generated route tree (Story 1.3 Dev Notes §6 — regenerated on each dev/build)
shell-ui/src/routeTree.gen.ts

# tauri-specta generated TS client (Story 1.4 — regenerated by prebuild via cargo test --test export_bindings)
shell-ui/src/lib/tauri.ts
