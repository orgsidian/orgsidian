# Deferred Work

## Deferred from: code review of story-1.1 (2026-05-19)

- **CSP disabled in `src-tauri/tauri.conf.json:22`** [MED] — `"csp": null` ships the app with no Content-Security-Policy; scaffold default. Harden before v0.1 Alpha (broad XSS surface once org-mode content rendering lands in Story 3+).
- **`vite.config.ts:5` `@ts-expect-error` will itself fail compilation if `@types/node` is later added** [MED] — directive becomes "unused" once the error it expects no longer occurs. Revisit when adding @types/node (Story 1.3 frontend wiring may force this).
- **`tsconfig.node.json:3` `composite: true` without `declaration`/`outDir`** [LOW] — `tsc -b` would write `.d.ts` next to `vite.config.ts`. Scaffold-emitted; revisit in Story 1.3 build-config retune.
- **`tsconfig.json` `references` block is dead under `tsc` (only honored by `tsc -b`)** [LOW] — `package.json:34` runs bare `tsc`, so `vite.config.ts` type-checking does NOT happen at build time. Story 1.3.
- **`src/App.tsx:11` `invoke('greet')` unhandled promise rejection** [LOW] — demo UI; Story 1.3 replaces.
- **`src/main.tsx:5` `document.getElementById("root") as HTMLElement` cast hides null** [LOW] — scaffold; Story 1.3.
- **`src/App.tsx:18-26,30` `<a target="_blank">` without `rel="noopener noreferrer"`** [LOW] — demo UI; Story 1.3.
- **`src/App.tsx:39-43` `<input>` half-controlled (no `value` binding)** [LOW] — demo UI; Story 1.3.
- **`src/App.tsx:39-43` `<input>` missing `<label>`/`aria-label`** [LOW] — demo UI accessibility; Story 1.3.
- **`.gitignore` ignores `src-tauri/gen/schemas/` but `src-tauri/capabilities/default.json:2` `$schema` references that path** [LOW] — editor JSON-schema warnings on fresh checkout until first `tauri dev`/`build`. Cosmetic; expected behavior.
- **`.gitignore` only ignores `src-tauri/gen/schemas/`, not future `gen/android/` or `gen/apple/`** [LOW] — mobile targets out of scope until post-v1.0; flag for when mobile is added.

## Deferred from: code review of story-1.2 (2026-05-21)

- **`crates/orgsidian-shell-app/Cargo.toml` shown as new file rather than rename** [LOW] — Content diverged enough during refactor that git rename detection fell below similarity threshold, breaking the blame trail on this single file. Other Tauri files (build.rs, icons, tauri.conf.json, src/lib.rs, src/main.rs) preserved rename history correctly. Cannot retroactively fix without rewriting history.

## Deferred from: code review of story-1.3 (2026-05-21)

- **`shell-ui/src/routes/__root.tsx` does NOT mount `<TooltipProvider>`** [MED] — The first story that mounts a `<Tooltip>` consumer must add `<TooltipProvider>` either at `__root.tsx` (preferred — global) or scoped to the consumer. Without it, Radix Tooltip throws "`Tooltip` must be used within `TooltipProvider`" on first use. Owner: first story to render a tooltip.
- **`shell-ui/src/components/ui/sonner.tsx:14` calls `useTheme()` from `next-themes` outside a `<ThemeProvider>`** [LOW] — degrades to `theme: undefined` → destructure default `"system"` until ThemeProvider wraps the tree. No real toasts are triggered in Story 1.3 so the effect is latent. Owner: Story 6.7 (themes) — wire `<ThemeProvider>` at app root.
- **`shell-ui/src/routes/_layout/today.tsx` has no parent `_layout.tsx` — pathless group children parent to root; adding `_layout.tsx` later retroactively reparents every existing child** [MED] — Story 1.3 spec explicitly designs this as intentional (line 112). Owner: Story 7.1 (Today Dashboard) — when introducing the real layout file, audit every existing child of `routes/_layout/` for layout-wrapping assumptions.

## Deferred from: code review of story-1.4 (2026-05-21)

- **`prebuild` couples frontend build to full Rust toolchain + Tauri compile** [`shell-ui/package.json:8`] [MED] — chosen Option A.3 per spec §Generation timing; full backend compile runs on every `pnpm --filter shell-ui build`. Slow on cold CI caches and requires Rust on TS-only contributor machines. Owner: revisit if CI build time becomes a constraint or if TS-only contributor path becomes a frequent ask.
- **AC9 round-trip validated manually only (no automated end-to-end gate)** [LOW] — deferred to Story 1.7/1.8 (CI scaffolding). Risk: a future regression that breaks `commands.ping()` round-trip will not be caught until manual verification of the next story. Owner: Story 1.7 / 1.8.
- **`Builder::<tauri::Wry>::new()` hardcodes desktop runtime** [`crates/orgsidian-shell-app/src/lib.rs:23`] [LOW] — mobile-incompat; v0.1 is desktop-only. Owner: post-v1.0 mobile story.
- **`thiserror = "1"` floats while transitive `thiserror 2.0.18` is pulled in by tauri-specta** [`Cargo.toml`, `Cargo.lock:160`] [LOW] — two major versions compile (bloat). Owner: Story 1.7 (cargo-deny / cargo-audit).
- **Unmaintained `Inflector` 0.11.4 transitive (last released 2018)** [`Cargo.lock:20-24`] [LOW] — pulled in via `specta-macros`. cargo-audit will flag. Owner: Story 1.7.
- **`pnpm tauri dev` does not run `prebuild` → stale `tauri.ts` race window when commands change** [LOW] — HMR + binary boot reconcile within seconds. Owner: workflow-ergonomics polish story.
- **Concurrent `pnpm tauri dev` + `cargo test --test export_bindings` can torn-write `tauri.ts`** [LOW] — theoretical race; tauri-specta upstream does not do atomic rename. Owner: monitor upstream tauri-specta or workaround via a lockfile in shell-ui prebuild.
- **`setup` closure borrows `specta_builder` after `invoke_handler()` call; pattern not test-asserted** [`crates/orgsidian-shell-app/src/lib.rs:62-67`] [LOW] — depends on tauri-specta API stability across upgrades. Owner: Story 1.7/1.8 (add a smoke test that exercises `commands.ping()` via Tauri's webview in CI).
- **`rm tauri.ts && pnpm tsc` (bare typecheck) reports cryptic missing-module from `today.tsx:3`** [LOW] — documented contributor flow is `pnpm --filter shell-ui build` (triggers `prebuild`). Owner: tech-writer docs or a `pnpm typecheck` script that depends on `prebuild`.
- **`#[serde(tag = "kind")]` on `OrgError` may not round-trip if `Deserialize` is derived later** [`crates/orgsidian-core/src/error.rs:15-17`] [LOW] — `OrgError` is `Serialize`-only by spec. Owner: revisit only if plugin-host deserialization path appears.
- **AC1 spec text omits `features = ["derive"]` requirement on `specta` (disclosed deviation #5)** [LOW] — Completion Notes disclose; epic update could amend spec wording. Owner: next epic-update touching Epic 1 AC text.
- **`tests/export_bindings.rs` does not assert on generated content (only that `export()` does not panic)** [`crates/orgsidian-shell-app/tests/export_bindings.rs`] [MED] — a regression dropping `OrgError` from the bindings or changing the `kind` discriminator would pass green. Owner: Story 1.7/1.8 — promote to a snapshot/golden-file test.

## Deferred from: code review of story-1.7 (2026-05-22)

- **Lockstep cargo↔JS allowlists not enforced** [`deny.toml`, `scripts/check-pnpm-licenses.mjs`, `docs/security/advisory-exceptions.md`] [MED] — Documented as "kept in lockstep" but enforced only by goodwill. Today the two sides intentionally diverge (cargo has `Unicode-3.0`/`BSL-1.0`/`Apache-2.0 WITH LLVM-exception`; JS has `0BSD`/`CC-BY-4.0`). Owner: Story 1.8 — add cross-file sync check as CI workflow step.
- **`cargo audit` does not honor `deny.toml [advisories].ignore`** [LOW-MED] — Separate tools with separate config surfaces. On a Linux runner RUSTSEC-2024-0429 (glib 0.18.5 unsoundness) may surface and fail `cargo audit` while `cargo deny check advisories` passes. Owner: Story 1.8 — wire CI to pass `--ignore RUSTSEC-2024-0429` to `cargo audit` and keep the flag list in lockstep with `docs/security/advisory-exceptions.md`.
- **`scripts/check-pnpm-licenses.mjs` is 85 lines vs AC9 budget `<80 LOC`** [`scripts/check-pnpm-licenses.mjs`] [NIT] — undisclosed minor deviation; likely shrinks during SPDX rewrite (Review patch #1). Otherwise disclose as deviation #11 in Completion Notes.
- **`deny-sources` alias missing from `.cargo/config.toml`** [`.cargo/config.toml:13-17`] [LOW] — AC8 binds the 4-alias set; `cargo deny check sources` is rare enough to hand-type. Owner: future hardening pass.
- **`[bans].skip` entries lack drift signal** [`deny.toml:133-165`] [LOW] — cargo-deny does NOT support `expiration` on `[bans].skip` (only on advisories). Quarterly review must come via Story 1.8 CI scheduled re-eval or human-only ledger discipline. Owner: Story 1.8 (CI scheduled job) or Story 1.13 (when GitHub repo lands).
- **`orgsidian-core` workspace dep hard-codes `version = "0.0.0"`** [`Cargo.toml:39`] [MED] — Will collide on the first `workspace.package.version` bump (e.g. → `0.1.0-alpha.1`). Disclosed deviation #7 acknowledges the pin; the future two-place edit is left as a `# Keep in sync with workspace.package.version` reminder. Owner: v0.1 Alpha version-bump story.

## Deferred from: code review of story-1.9 (2026-05-25)

- **Watcher anchor hang risk on writer panic + mtime-resolution flake on coarse filesystems** [`crates/orgsidian-watcher/tests/anchor.rs`] [MED] — The anchor relies on a `FakeClock` that never advances; if the writer thread's `std::fs::write().expect(...)` panics (disk full, EPERM, signal) the detector's mtime poll loop never observes a change AND the FakeClock-driven deadline never fires → `cargo test` hangs indefinitely (no stable `#[test]` timeout in Cargo). Same hang on coarse-resolution filesystems (HFS+ 1s, FAT 2s, some NFS) where the 50ms writer delay can't disambiguate from `initial_mtime`. Dev Notes §9 anticipates the resolution path. Owner: Story 5.1 (notify-rs replaces the polling body and the FakeClock-driven hang surface entirely).
- **`metadata().modified()` race vs. `renameat` from `atomic-write-file` in the polling detector** [`crates/orgsidian-watcher/src/lib.rs:60`] [LOW] — `std::fs::metadata(path)?` between the initial reading and a concurrent `atomic-write-file::commit()` (which does `renameat`) can race and return `ENOENT` → `DetectError::Io(NotFound)` instead of `Ok(DetectedEvent)`. Anchor uses `std::fs::write` (truncate-in-place, not rename) so latent. Owner: Story 5.1 (notify-rs event subscription replaces the polling body and resolves the race in one stroke).
- **Temp-file leak on `write_all` error path in `atomic_write`** [`crates/orgsidian-vault/src/lib.rs:18-22`] [LOW] — If `AtomicWriteFile::open` succeeds but `write_all` fails, the temp-sibling-file cleanup depends on `AtomicWriteFile`'s `Drop` impl (not documented by the crate). On cross-device targets (tmpfs/bind-mount) `commit()` returns `EXDEV` and the temp may persist next to the target. Anchor's one-shot happy path doesn't exercise. Owner: Story 3.1 (AV-aware retry wrapper + production error-path semantics).
- **`atomic_write` returns bare `io::Error` for `EISDIR` / `ENOENT` (no context wrapping)** [`crates/orgsidian-vault/src/lib.rs:18`] [LOW] — Caller can't distinguish "target path invalid" from "transient I/O" without `ErrorKind` introspection. AC2 explicitly bounds Story 1.9 to a single delegation chain. Owner: Story 3.1 (production-grade error context wrapping per LD-8).

## Hotfix: windows nightly export_bindings skip (2026-05-25)

- **`export_bindings` test skipped on Windows pending IPC-contract crate extraction** [`crates/orgsidian-shell-app/tests/export_bindings.rs`] [MED] — Test exe transitively links `webview2-com-sys 0.38.2` via `Builder<tauri::Wry>` and fails at process load on `windows-2022` with `STATUS_ENTRYPOINT_NOT_FOUND` (0xc0000139). Surfaced by Story 1.8's nightly Windows matrix; latent since Story 1.4. Short-term: `#[cfg_attr(windows, ignore = "...")]` (later hardened to module-level `#![cfg(not(target_os = "windows"))]` in #122 + nightly.yml `if: matrix.os != 'windows-2022'` skips on `Build shell-ui` + `i18n catalog drift check` in #124, since the test was also the `tauri.ts` regen step). Proper fix: extract `build_specta()` into a new `orgsidian-ipc-contract` crate depending only on `tauri-specta + specta + specta-typescript` (no `tauri::Wry`); once landed, remove all four Windows skips. Tracked at [#120](https://github.com/orgsidian/orgsidian/issues/120). Owner: focused crate-extraction story before v0.5 Beta's plugin-API lock.

## Follow-up: stale "repo private during pre-Alpha" framing (2026-05-25)

- **`README.md:7` asserts "Repository is private during pre-Alpha"** [LOW] — repo is in fact public (verified 2026-05-25 via `gh repo view --json visibility` returning `PUBLIC`); LD-5 "private until v0.1 Alpha flip" wording is also stale. Surfaced during Story 1.10 (`bmad-create-story`) — the four new root docs (SECURITY/ARCHITECTURE/CHANGELOG/CONTRIBUTING) were corrected to drop the stale framing, but README.md was kept out of Story 1.10's scope-fence (AC7) to avoid scope creep. Fix: one-line edit to README.md status line + optional one-line note that LD-5's repo-visibility-timing clause is stale (architecture itself is not in scope for amendment here). Owner: next docs-touching story, or a standalone `docs(readme): drop stale "private" framing` commit (5-min job). Memory anchor: [[project-orgsidian-repo-public-during-pre-alpha]].

## Deferred from: code review of story-1.11 (2026-05-26)

- **`disk_full_atomic_write` exemplar teaches wrong `fail::cfg` pattern** [`tests/failure_modes.rs:43-50`] [LOW] — Spec AC1 dictates verbatim `fail::cfg("atomic-write::after-tmp-rename", "panic")` + `assert!(result.is_err())`; the `"panic"` action unwinds the call stack so the `is_err()` assertion is unreachable. Correct pattern: `fail::cfg("...", "return")` with a `fail_point!(...)` that converts the trigger into `Err(...)`. Defer rationale: spec AC1 dictates the exemplar verbatim; Story 3.1 will rewrite the body when implementing real fault injection — patching now would deviate from literal AC1. Owner: Story 3.1 (first LD-41 implementation, "Disk full / ENOSPC during atomic write").
- **Anti-placebo gap: gate does not tie `unimplemented!()` body presence to `#[ignore]` marker** [`tests/failure_modes_coverage.rs:22-52`] [LOW] — Removing BOTH `#[ignore]` and `unimplemented!()` (replacing body with `{}`) makes the test silently pass with zero assertions. Strengthening: `assert!(unimplemented_count == ignore_count)` in scan. Real LD-41 impl stories replace bodies anyway. Owner: first LD-41 implementation story (likely Story 3.1).
- **Strict-mode env-var advisory `eprintln!` invisible in CI; no workflow step runs strict mode** [`tests/failure_modes_coverage.rs:72-78` + `.github/workflows/*`] [MED] — `cargo test` swallows stderr from passing tests; no `--nocapture` / `--show-output` and no `ORGSIDIAN_FAILURE_MODE_STRICT=1` step in any workflow. Spec AC6 anti-creeps workflows. Owner: v0.5-Beta release-prep story (alongside strict-mode flip).
- **Cross-platform idempotency of `pnpm gen:failure-modes-matrix` not asserted in CI** [`scripts/gen-failure-modes-matrix.mjs:803-805` + nightly.yml absent step] [LOW-MED] — Windows PowerShell `>` redirect may emit CRLF/UTF-16; script emits LF only, file committed LF, but undetected on Windows regen. Right shape: `--check` mode + CI gate. Owner: future CI-hardening story.
- **`required-features = ["test-support"]` decorative; `cargo test -p orgsidian-core` silently skips harness** [`crates/orgsidian-core/Cargo.toml:32-35`] [LOW] — Harness file does not reference `test_support`; workspace invocation works only via `orgsidian-watcher`'s incidental dev-dep chain. Documented in Dev Notes §4 + Completion Notes as forward-looking trade-off. Owner: first LD-41 implementation story.
- **Stale placeholder comment in `nightly.yml` promises LD-41 nightly step that doesn't exist** [`.github/workflows/nightly.yml:~112`] [LOW] — Pre-existing comment with no actual step; Story 1.11 AC6 anti-creeps workflows. Owner: v0.5-Beta release-prep story.

## Deferred from: code review of story-1.8 (2026-05-23)

- **`invoke_plugin_hook!` macro is sync-only — cannot host `await`/`?`/early `return`** [`crates/orgsidian-core/src/registry.rs:99-122`] [LOW] — `catch_unwind(AssertUnwindSafe(|| $call))` wraps the call in a non-async closure. Document the constraint in the macro doc-comment now; ship a sibling `invoke_plugin_hook_async!` (using `FutureExt::catch_unwind`) when async hooks land. Owner: Epic 4+ when WASM v1.5 async plugin hooks materialize.

## Deferred from: code review of story-1.12 (2026-05-28)

- **Memoize `workspace_root()` via `LazyLock`** [`crates/orgsidian-core/src/test_support/perf.rs:81-98`] [LOW] — Walk-up reads `Cargo.toml` files on every impl call; for many perf gates per `cargo test` run this is sub-microsecond noise but it does add I/O variability to the very samples the macro gates. Cache the resolved path after first use. Owner: next perf-AC story that observes baseline jitter, or a focused perf-test-infra cleanup.
- **`current_runner_class()` duplicated between impl and self-tests** [`crates/orgsidian-core/src/test_support/perf.rs:71` ↔ `crates/orgsidian-core/tests/perf_canary.rs:17-19`] [LOW] — Forced by `pub(crate)` visibility. If `std::env::consts` formatting ever shifts, the test's local helper drifts and `perf_impl_treats_missing_runner_class_as_first_run` silently keeps passing. Promote `current_runner_class()` to `pub` so tests consume the real fn. Owner: next perf-test-infra cleanup.
- **Macro panic-message `pct` integer-floor rounding can be off by one** [`crates/orgsidian-core/src/test_support/perf.rs:281-289`] [LOW] — `(measured * 100 / baseline).saturating_sub(100)` rounds toward zero; cosmetic in the diagnostic only — the gate decision uses the correct `measured > baseline * 120 / 100` formula. Compute the percentage via `(measured.saturating_sub(baseline) * 100) / baseline` if a follow-up tightens the panic-message accuracy.
- **`current_iso8601_utc()` silently maps pre-UNIX-epoch clocks to `1970-01-01T00:00:00Z`** [`crates/orgsidian-core/src/test_support/perf.rs:101-105`] [LOW] — `duration_since(UNIX_EPOCH).map(...).unwrap_or(0)` swallows clock skew. `created_at` is forensic metadata only (the gate never reads it), so this is cosmetic — but a misconfigured CI runner would silently write `1970-01-01` baselines. Surface as a warning on the `Err` branch in a follow-up.
- **Self-test assertions compare `serde_json::Value` against `100u64` literals** [`crates/orgsidian-core/tests/perf_canary.rs:38-40,97-100`] [LOW] — Works today because `Value` implements `PartialEq<u64>`; brittle to a future schema change that ever serializes `median_ns` as a float. Switch to typed deserialization into `BaselineFile` (or `.as_u64()`) for robustness. Owner: next perf-test-infra cleanup.

## Deferred from: code review of story-1.13 (2026-05-28)

- **Issue template label drift** [`.github/ISSUE_TEMPLATE/story.md:5`] [LOW] — Legacy `.md` template hard-codes `labels: ['type:story', 'status:backlog']`; if either is renamed in `labels.yml` later, GitHub silently fails to apply at issue-create (no warning, no error). No drift-detection mechanism. Owner: revisit when migrating to issue-forms (`.yml`) format or when LD-55 label scheme evolves.
- **Label-state inconsistency on PR-close** [Story 1.13 Task 9.3] [LOW] — `Closes #N` auto-closes the issue but doesn't flip `status:in-progress` → `status:done`. Maintainer must run the label flip manually. Owner: Story 1.16 (Rust issue-sync binary) can wire as a CI step on PR-merge.
- **Project v2 board state not reproducible from repo** [LOW] — Board #1 ("Orgsidian Roadmap") + Status field reconfig (Backlog/In Progress/Review/Done) exist only in GitHub state; no script/Makefile encapsulates creation. Bootstrap is narrative-only in the story doc. Owner: revisit if onboarding cost matters or board state is lost.
- **No `status:*` mutual-exclusion enforcement** [`.github/labels.yml`] [LOW] — Issues can carry `status:backlog` AND `status:done` simultaneously; LD-55 assumes mutual exclusion but doesn't enforce. Owner: Story 1.16 sync binary could validate; or process discipline.
- **`scripts/sync-epics-to-github.sh` lacks preflight label-existence check** [`scripts/sync-epics-to-github.sh:163`] [LOW] — Post-AC6 decommission of `ensure_label` invocations, a fresh clone running the script before `labels-sync` ever fired would `gh issue create` with labels that don't exist → 422 per issue. Mitigated because Story 1.16 retires this script entirely. Owner: Story 1.16.
- **No `priority:p2`/`priority:p3` fallback label** [`.github/labels.yml:142-147`] [LOW] — Only `p0`/`p1` declared per LD-55 "used sparingly". Queries `label:"priority:*"` filter out the unlabeled majority. Owner: LD-55 design discussion if the priority taxonomy expands.

## Deferred from: code review of story-1.16 (2026-05-29)

- **Renderer hardcodes `https://github.com/orgsidian/orgsidian/...` in `**Source:**` URL** [`tools/issues-sync/src/render.rs:25-43`] [LOW] — Spec didn't mandate parameterization; current invocation always uses `orgsidian/orgsidian`. A fork/staging org test would link to the wrong repo. Owner: any story that introduces multi-target sync (none planned).
- **Golden file is renderer self-snapshot, not live `gh issue view 1` byte-snapshot** [`tools/issues-sync/tests/golden/story-1-1-body.md`] [LOW] — Spec Task 3.2 instructed committing the live body verbatim; PR regenerated from the renderer (acknowledged in Dev Agent Record §B + Debug Log row 6). True byte-stability is only validated post-first-run via AC10 cell 6. Owner: follow-up after first real `sync-issues.yml` green run.
- **`expected_labels_for_story` removes any non-status manual label** [`tools/issues-sync/src/sync.rs expected_labels_for_story`] [LOW] — Spec-compliant ("status:* preserved" is the explicit invariant), but a maintainer adding `pinned`/`area:editor` will see it stripped on next sync. No allowlist. Owner: revisit if a non-managed-label use case emerges.
- **No CODEOWNERS entry for `tools/issues-sync/`** [governance] [MED] — Workflow holds `secrets.PROJECTS_PAT` (org-level Projects R/W); under GitHub Free branch protection is unenforceable per `[[project_orgsidian_github_plan]]`. A merged PR that adds `print!("{}", env!("GITHUB_TOKEN"))` would exfiltrate the PAT on next push-to-main. Owner: governance/CODEOWNERS story (separate from LD-55).
- **`workflow_dispatch` lacks `dry_run` input** [`.github/workflows/sync-issues.yml:53`] [LOW] — Dry-run is local-only today; maintainers cannot preview from the workflow UI. Owner: minor DX follow-up.
- **`dtolnay/rust-toolchain@stable` is a floating branch reference** [`.github/workflows/sync-issues.yml:72`] [LOW] — Version-pin discipline drift (per `[[feedback_version_policy]]`); matches existing `pr.yml` pattern so pre-existing convention. Owner: workflow-hardening pass.
- **No CRLF normalization in parser** [`tools/issues-sync/src/parser.rs`] [LOW] — Repo files are LF + `.gitattributes` enforces; a Windows contributor bypassing autocrlf would cause permanent body diffs. Owner: defensive hardening when first CRLF symptom appears.
- **Code-fenced `### Story` headings match parser regex** [`tools/issues-sync/src/parser.rs:140-148`] [LOW] — Parser has no fence-state tracking. No docs/meta story today; would fire for a story that demonstrates story format. Owner: when first such story is drafted.
- **Mis-nested story takes heading's epic, not enclosing section's** [`tools/issues-sync/src/parser.rs:145`] [LOW] — `caps[1].parse().unwrap_or(epic)` silently accepts wrong-epic heading inside the wrong section. Data-entry error class. Add `debug_assert_eq!(story_epic, section_epic)` later. Owner: defensive hardening.
- **`epic: u8` overflow for Epic 256+** [`tools/issues-sync/src/parser.rs:118`] [NIT] — Max planned epic ≤ 13. Owner: dismiss-on-sight.
- **Trailing post-Epic-13 content swallowed into last story body** [`tools/issues-sync/src/parser.rs` flush-at-EOF] [LOW] — File currently ends cleanly. Defensive fix: require trailing sentinel or `## End`. Owner: defensive hardening.
- **Sentinel-line migration causes ~117 body PATCHes on first real run** [`tools/issues-sync/src/render.rs:25`] [MED-by-impact-low-by-spec] — Documented one-time churn (Dev Agent Record §C). Concern: combined with absent retry/backoff, the run may abort mid-migration. Owner: ties to AC4 retry decision.
- **Crash mid-run leaves inconsistent state — no resume token** [`tools/issues-sync/src/sync.rs:137-173`] [MED] — Convergence-on-next-run is the documented contract; without retry/backoff (Patch #1 above) the recovery window may be long. Owner: ties to AC4 retry decision.
- ~~**`extract_traces` `trim_start()` matches indented bullets containing `**Traces:**`**~~ — _Promoted to patch during review wrap-up: surfaced by the `extract_ac_block_ignores_earlier_lookalike_terminator` regression test; fixed to column-0-only match in [tools/issues-sync/src/parser.rs](tools/issues-sync/src/parser.rs)._
- **`expected_milestone_num: None` silently drops milestone on create/reconcile** [`tools/issues-sync/src/sync.rs:142`] [LOW] — Defensive case requiring partial-fail in `ensure_milestones`; abort-on-error covers the steady state. Owner: defensive hardening.
- **`partition_labels` accepts the literal `"status:"` (no suffix) into the preserved set** [`tools/issues-sync/src/sync.rs partition_labels`] [NIT] — Trigger: maintainer creates a bare `status:` label. Cosmetic. Owner: dismiss-on-sight.

## Deferred from: code review of story-1.17 (2026-06-01)

- **Selector regex `[^}]*` in `contrast.test.ts` cannot survive nested braces** [`shell-ui/src/themes/contrast.test.ts:parseTokens`] [LOW] — Story 6.7 may introduce Tailwind 4 idioms like `@media` nesting inside `:root`. Today's tokens.css doesn't trigger; future-fragility flagged for Story 6.7.
- **Dark theme `.dark` block is gate #1-validated but unreachable at runtime** [`shell-ui/src/themes/tokens.css`] [LOW] — No theme toggle wired (next-themes is in deps but no provider). Gates #2/#3 give zero dark-mode axe coverage until a toggle ships. Defer to Story 6.7 / theme-toggle work.
- **Workflow Step 14 → Step 16 ordering is load-bearing but undocumented** [`.github/workflows/pr.yml`] [LOW] — Dev server boot depends on Vite/route/lingui generated files from `Build shell-ui`; reordering would silently break the gate (vacuous fixme pass). Add inline comment in next `pr.yml` refactor.
- **PR #135 sprint-status.yaml flips 1-10 and 1-14 to `done` orthogonally to Story 1.17 scope** [`_bmad-output/implementation-artifacts/sprint-status.yaml`] [NIT] — Sprint-tracking hygiene: reverting PR #135 would regress unrelated story statuses. Separate sprint-management concern.
- **No `actions/upload-artifact` step for Playwright traces/HTML reports on CI failure** [`.github/workflows/pr.yml`] [LOW] — Quality-of-life debugging once downstream stories un-fixme scenarios. Add when a real failure mode surfaces.

## Deferred from: code review of story-1.18 (2026-06-02)

- **`toml_edit` migration for format-preserving GUI round-trip** [`crates/orgsidian-core/src/settings/vault.rs`, `global.rs`] [MED] — Story 1.18 uses the structural `toml` crate (1.x); user-added comments and key-reordering in the on-disk file are LOST through a `write_*_settings` call. Acceptable for v0.1 (no GUI Settings editor yet). Migrate to `toml_edit` (0.25.x) when Story 12.3 lands the FR-23 keybinding remap UI — that's the first user-driven write path that surfaces the limitation. `// FOLLOWUP(Story-12.3): swap to toml_edit ...` markers exist in both writer modules.
- **LD-41 backup-and-warn fallback for malformed settings.toml** [`crates/orgsidian-shell-app/src/lib.rs setup`, Story 6.7+] [MED] — When `read_global_settings()` returns `SettingsError::ParseFailed`, the current bootstrap just `tracing::warn!`s and continues with defaults. Production behavior should rename `<file>.broken-{timestamp}` and surface a user-visible banner so the next write doesn't clobber the user's broken-but-recoverable hand-edits. Owner: Story 6.7 (or earlier if a real corruption surfaces).
- **Architecture FR-23 row at `_bmad-output/planning-artifacts/architecture.md#L1068`** [planning-artifacts] [NIT] — Lists `tauri-plugin-store` as a co-implementer of FR-23. Stale per the 2026-05-20 LD-40 amendment. Owner: docs-sweep story that touches the archival architecture.md.
- **GUI consumption of read/write settings** [Stories 12.3 / 11.5 / 7.2 / 7.5 / 8.1 / 11.3 / 6.7] — Story 1.18 ships only the boot-time smoke; no Tauri `#[tauri::command]` wrappers, no Zustand store wiring, no Settings panels. Each downstream Settings-touching story consumes `read/write_*_settings` from day 1 per LD-40 — flag any PR that reaches for `tauri-plugin-store` outside the closed allowlist in [`docs/architecture/settings-boundary.md`](../../docs/architecture/settings-boundary.md).
- **Per-file watcher reload hook for settings.toml** [`crates/orgsidian-core/src/settings/vault.rs`, Story 5.4] [MED] — settings.toml is just another file under the watcher; external edits should reload via the LD-7 dirty-buffer check. Story 1.18 leaves a `// FOLLOWUP(Story-5.4): watcher reload hook lands here per LD-7 Single Writer Rule` marker; Story 5.4 wires the actual subscription.

## Deferred from: code review of story-2.1 (2026-06-03)

- **`grammar::language()` is `pub(crate)`, not `pub`** [`crates/orgsidian-parser/src/grammar/mod.rs`] [LOW] — Story 2.1 keeps the FFI accessor crate-private; the public surface promise is the `parse() -> ParseTree` wrapper only. Story 2.2 promotes to `pub` only if its design needs raw `Language` access; current expectation is that `parse()` consumes `language()` internally only.
- **`_language_for_smoke` shim** [`crates/orgsidian-parser/src/lib.rs`] [LOW] — `#[doc(hidden)] pub fn _language_for_smoke()` is the AC4 anti-placebo-green compromise (`#[cfg(test)] pub use` is invisible to integration tests; `pub use` of a `pub(crate)` item is forbidden by E0364). Story 2.2 deletes the shim when `parse()` consumes `language()` directly and `tests/grammar_link.rs` is replaced by the real `parse()` body test.
- **Submodule init in README quickstart** [`README.md`] [LOW] — First-time-builder instructions should mention `git submodule update --init --recursive`. Out of Story 2.1 scope (docs-sweep story owns README touch); `build.rs` already hard-fails with a parser-owner-readable message if the submodule is missing.
- **Architecture `pushed_at` vs commit-HEAD divergence on tree-sitter-org** [`_bmad-output/planning-artifacts/architecture.md#L65` LD-3] [NIT] — Architecture says "last push 2026-05-05" but `commits/main` HEAD is `219c0b27fd` dated 2025-02-23. The `pushed_at` reflects tag/branch activity, not new code commits — code-wise the upstream has been quiet ~15 months. NOT a Story 2.1 blocker, but flag for the v0.3 fork-and-maintain dry run (LD-48).
- **Epic AC wording mentions `tree-sitter-cli` build hooks** [`_bmad-output/planning-artifacts/epics.md#L761`] [NIT] — Actual mechanism is `cc::Build::new()` from a Rust `build.rs`; no `tree-sitter-cli` binary executes at build time. Architecture/epic rewording deferred — epics.md is the sync-source for GitHub issues, mid-epic rewording risks churn.
- **`scanner.c` existence-check is defensive** [`crates/orgsidian-parser/build.rs`] [NIT] — The pinned `nvim-orgmode/tree-sitter-org` SHA DOES ship a `scanner.c`. The `if scanner_c.exists()` guard is free insurance against a future grammar that drops external tokens; parser owner may simplify to unconditional `.file(&scanner_c)` if judged over-cautious.
- **CI rust-cache cold-build timing** [`_bmad-output/planning-artifacts/architecture.md#L1452` LD-12 perf-canary] — Story 2.1 cold-cache `cargo build -p orgsidian-parser --locked` measured at **5.15s** on macOS-arm64 (Apple Silicon, local). First CI run after merge will pay this once; subsequent runs hit `Swatinem/rust-cache@v2`. Capture the Ubuntu-LTS + Windows-2022 numbers from the first nightly run as the perf-canary baseline.
- **Upstream `scanner.c` sign-compare warnings** [`grammar/src/scanner.c:84-292`] [LOW] — `cc` emits 5 `-Wsign-compare` warnings from upstream code. Suppressed via `flag_if_supported("-Wno-unused-parameter|unused-but-set-variable|trigraphs")` only; the sign-compare line is unsuppressed. Out of scope to patch (upstream code; LD-48 vendoring discipline forbids local edits). Owner: upstream PR if it ever blocks.
- **`_language_for_smoke` is `pub` un-gated (public-API leak)** [`crates/orgsidian-parser/src/lib.rs:50-53`] [LOW] — Already tracked above as the AC4 deviation; Story 2.2 deletes the shim. Re-surfaced in code-review for visibility: anything `pub` is part of the crate's stable API regardless of `#[doc(hidden)]`, and removing it in Story 2.2 is technically a breaking change. Per the Debug Log this is the technically-mandatory deviation (E0364 + `#[cfg(test)]` invisibility to integration tests); confirmed acceptable.
- **Parser-owner SHA-review checklist should enumerate `.gitmodules` diff in the upstream submodule** [`CONTRIBUTING.md` §8] [NIT] — `submodules: recursive` on `actions/checkout@v5` will silently expand the vendored surface if upstream `tree-sitter-org` ever introduces nested submodules. Today upstream has no nested submodules so impact is theoretical. Owner: v0.3 fork-and-maintain dry run (LD-48) — add `.gitmodules` diff to the §8 SHA-review checklist alongside grammar.js/scanner.c/test-corpus/node-types.
- **No MSRV pin in workspace Cargo.toml** [`Cargo.toml`] [LOW] — `rust-version` is intentionally omitted (Story 1.2). The new `unsafe extern "C" { … }` block in `crates/orgsidian-parser/src/grammar/mod.rs:12` requires rustc ≥ 1.82 (Rust-2024 form). `rust-toolchain.toml` pins `channel = "stable"` so CI is auto-current, but a contributor with a stale local `stable` channel hits a confusing parse error with no MSRV floor to anchor the message. Owner: MSRV-policy story.

## Deferred from: code review of story-2.2 (2026-06-10)

- **Parser-per-call allocation (no pooled/thread-local `Parser` reuse)** [`crates/orgsidian-parser/src/lib.rs`] [LOW] — `parse()` constructs a fresh `tree_sitter::Parser` per call. Correct and simple for the wrapper (parsing requires `&mut Parser`, so per-call construction avoids shared mutable state; note `Parser` is actually `Send + Sync` at tree-sitter 0.26.9 — review correction 2026-06-10). Revisit with a thread-local pool only if Story 2.3+ profiling shows hot-path cost — for typical file sizes parser construction is negligible vs the parse itself.
- **`ERROR`/`MISSING` node tolerance is intentional at the wrapper layer** [`crates/orgsidian-parser/src/lib.rs`, `tests/grammar.rs`] [NIT] — `parse()` returns `Ok` for any source that yields a tree; malformed constructs surface as `ERROR`/`MISSING` nodes inside a valid `document`, not as `Err`. Fidelity is enforced later by the Story 2.6 L0 round-trip gate (and the PRD FR-1 plain-text fallback posture depends on this tolerance). Not a gap to "fix" here.
- **Vendored `scanner.c` list-indent state serialized as signed `char` — valid org with cumulative indent ≥128 columns misparses into `ERROR` nodes** [`crates/orgsidian-parser/grammar/src/scanner.c:75-101`] [MED] — Empirically verified at the pinned SHA (`219c0b27…`): list indent 127 → clean tree, indent 128 → `has_error=true`; any nesting whose cumulative indent crosses 128 (≈64 levels at 2-space steps; error confirmed at depth 80) corrupts scanner state on serialize/deserialize. `parse()` returns `Ok` with `ERROR` nodes for *well-formed* input — silent fidelity loss the Story 2.6 L0 round-trip gate will hit. LD-48 vendoring discipline forbids local edits; owner: upstream PR / v0.3 fork-and-maintain dry run (LD-48). Breadcrumb: signed-byte boundary in `serialize`/`deserialize` of indent stack.
- **No incremental-reparse or cancellation path reachable through the public surface** [`crates/orgsidian-parser/src/lib.rs:67-72`] [LOW] — `parse(source, None)` hardcodes a full reparse (`old_tree = None`) and `grammar::language()` stays `pub(crate)`, so downstream cannot combine the `pub use tree_sitter` re-export with the org grammar to build a configured `Parser` (incremental edits, timeout/cancellation flags). Measured ~1.4 s uncancelable block on a 10 MB org file. Consequence: `ParseError::NoTree` is de facto unreachable while the capability it documents cannot be exercised. Owner: Story 2.3+ (editor-loop consumer) — extend the wrapper API only when a real consumer needs it (pairs with the parser-pooling item above). _Story 2.3 decision (2026-06-10): explicitly NOT taken — `analyze()` is a batch consumer; no editor consumer exists yet, the `parse()` wrapper surface is untouched (`grammar::language()` stays `pub(crate)`, no pooling, no incremental/cancellation API). Stays deferred until a real editor-loop consumer (Epic 4+)._
- **`ParseTree` source-retention design (owned `String` field vs `ParseTree<'a>` borrow vs caller-carried)** [`crates/orgsidian-parser/src/lib.rs:24-44`] [MED] — `tree_sitter::Tree` stores byte offsets only; resolving node text requires the exact source passed to `parse()`, byte-identical and kept alive, with nothing currently tying the two together. Structural fix deferred (review decision 2026-06-10): zero consumers exist today, the anchor sentinel locks the `parse()` signature — not `ParseTree` internals — and the owned-vs-borrowed trade-off (String copy doubles memory on large files vs lifetime infecting downstream APIs) should be decided by Story 2.3's real semantic-layer consumer. Meanwhile the keep-the-source contract is documented on `ParseTree`/`parse()`. Owner: Story 2.3 API design. _Resolved by Story 2.3 (2026-06-10): semantic structs OWN their data — extracted `String`s + `Range<usize>` spans into the `analyze()` input; no lifetime parameters in the public semantic surface; `ParseTree` internals untouched. Rationale: lifetimes would infect every downstream API (index, IPC, editor); owned-extraction memory cost is bounded by file size and acceptable per the LD-42 large-vault posture. The keep-the-source contract is now internal to `analyze()`._

## Deferred from: code review of story-2.3 (2026-06-10)

- **Inline markup / citations / inline-LaTeX semantic modeling** [`crates/orgsidian-parser/src/semantic/`] [LOW] — The grammar leaves all three as `expr` soup (KNOWN_DIVERGENCES.md entries 2-4); Story 2.3 tolerates them as raw text with spans preserved. An Epic 4 rendering consumer decides whether the semantic layer grows an inline pass or the renderer does its own. Owner: Epic 4 consumer decides.
- **Link-grammar completeness vs org-element** [`crates/orgsidian-parser/src/semantic/link.rs`] [LOW] — The hand-rolled scanner covers `[[…]]`/`[[…][…]]` + plain `http(s)://`; angle links (`<http://…>`), link abbreviations (`#+LINK:`), `~/` file expansion, and non-http plain schemes are out of scope (KNOWN_DIVERGENCES.md entry 1). Owner: Epic 4 link navigation, if it needs more.
- **Multi-`#+TODO:`-sequence cycling edge semantics** [`crates/orgsidian-parser/src/semantic/todo.rs`] [LOW] — Multiple directives accumulate (org-faithful) and `next()` cycles within the keyword's own sequence; a keyword appearing in TWO sequences resolves to the first declaring sequence, and `next(None)` always starts at the first sequence. Vanilla org's fast-access-key selection UI semantics are not modeled (suffixes are stripped). Owner: revisit when Epic 4 wires the cycling keybinding.
- **Body-paragraph timestamp extraction** [`crates/orgsidian-parser/src/semantic/`] [LOW] — Timestamps outside plan position are `expr` soup (KNOWN_DIVERGENCES.md entry 7) and are not surfaced semantically. Agenda-style features need a body-text timestamp scan (the `timestamp.rs` text parser is reusable as-is). Owner: Epic 5+ agenda consumer.
- **CLOCK lines scanned only in `:LOGBOOK:` drawers** [`crates/orgsidian-parser/src/semantic/drawer.rs`] [NIT] — org's `org-clock-into-drawer` can route clocks into custom drawers; Orgsidian parses CLOCK lines from LOGBOOK-named drawers only. Owner: revisit with clocking features.
- **Plan entries with custom names are dropped from the semantic surface** [`crates/orgsidian-parser/src/semantic/headline.rs`] [NIT] — The grammar's `entry_name` accepts any `\p{L}+`; only SCHEDULED/DEADLINE/CLOSED route into `Headline`. A custom plan entry (or a bare plan-line timestamp) is currently unreachable through the semantic API (raw span still covers it). Owner: dismiss-on-sight unless a consumer appears.
- **Link scan does not exclude verbatim regions** [`crates/orgsidian-parser/src/semantic/headline.rs:244-253`] [NIT] — (review finding 2026-06-10) The inline link scan runs textually over a headline's whole own-region, so link-shaped text inside `#+BEGIN_SRC`/`#+BEGIN_EXAMPLE` blocks, drawer contents, and property values is reported as links; org-element would not. Structural fix (subtracting `block`/`drawer` node ranges from the scan region) deferred — false positives are harmless to round-trip (spans/raw preserved) and only matter to clickable-link UX. Documented in KNOWN_DIVERGENCES.md entry 1 and on `Headline::links`. Owner: Epic 4 link navigation decides.
- **`Timestamp::end_time` is a single field shared by time-ranges and date-range second halves** [`crates/orgsidian-parser/src/semantic/timestamp.rs`] [NIT] — (review finding 2026-06-10) For degenerate `<d1 10:00-11:00>--<d2 t>` stamps a consumer cannot tell which date the end time attaches to (precedence documented on the field: second half wins, first-half range end kept otherwise; `raw` carries full fidelity). Splitting the field is an API-shape decision for the Story 2.4 serializer / Epic 4 picker consumer. Owner: Story 2.4+. _Story 2.4 decision (2026-06-10): the field split is NOT forced by 2.4 — the raw-passthrough serializer never consumes `Timestamp` fields (no timestamp re-rendering, by design); byte fidelity is carried by `Headline::raw`. The API-shape decision stays deferred to the Epic 4 timestamp-picker consumer._

## Deferred from: code review of story-2.4 (2026-06-10)

- **Mutation-aware serialization (edit one semantic field → emit the change)** [`crates/orgsidian-parser/src/serializer.rs`] [MED] — Story 2.4 ships raw-region passthrough only ("saved without user-visible edits → byte-identical", FR-2). Applying a semantic edit (e.g. cycle `todo_state`) and re-emitting the headline is Epic 4's edit-application concern; the serializer module doc marks it explicitly out of scope. Owner: Epic 4 edit-application story.
- **Corpus expansion to the LD-44 ~100-file subset** [`crates/orgsidian-parser/tests/fixtures/round_trip/`] [MED] — The interim 18-file corpus is the harness's own test diet; Story 2.5 ships the real subset (`fixtures/subset-pr.json`, corpus-extractor, git-LFS vault corpus) and plugs into the same directory-driven `round_trip_subset` test. Owner: Story 2.5/2.6. *(2026-06-11, Story 2.5: corpus + manifests + extractor delivered; harness repointing to `fixtures/subset-pr.json` remains with Story 2.6.)*
- **Gap-absorption on pathological trees is coverage-exact but shape-lossy** [`crates/orgsidian-parser/src/semantic/mod.rs`, `headline.rs`] [LOW] — Bytes from root-level `ERROR` nodes / headline-less sections are folded into the nearest retained `raw` (tiling invariant holds; arbitrary-input proptest enforces), but WHERE they get absorbed is an implementation detail, not a semantic claim. Any consumer that wants structured access to ERROR regions (diagnostics UI) needs its own surface. Grammar-bug territory (e.g. the scanner.c signed-char indent bug, story-2.2 stanza) stays upstream-owned. Owner: revisit only if a diagnostics consumer appears.
- **Recursion depth in the semantic walk + serializer is unbounded** [`crates/orgsidian-parser/src/serializer.rs`, `src/semantic/headline.rs`] [NIT] — (review finding 2026-06-10) `build_section` (pre-existing, Story 2.3), `emit`, and `absorb_trailing` all recurse once per nesting level; a pathologically deep headline tree (thousands of levels, e.g. adversarial input) could overflow the stack. Real vaults are depth-bounded (LD-42 posture) and the proptest generators never approach such depth. Not a 2.4 regression — the serializer mirrors the parser's established walk posture. Owner: revisit only if untrusted-input hardening becomes a requirement.

## Deferred from: code review of story-2.8 (2026-06-10)

- **Rest of the LD-27 command tree** [`crates/orgsidian-cli/src/cli.rs`] [MED] — Story 2.8 ships `parse` only; `index`, `query`, `validate-plugin`, and `vault` subcommands slot into the existing `Command` enum when their backing crates exist. Owner: Epics 3+ (each command lands with its first-use story, per the architecture "CLI as integration-test surface" rule).
- **`docs/cli.md` Markdown reference regeneration** [`crates/orgsidian-cli/build.rs`] [LOW] — The architecture CLI Documentation Strategy names a Markdown reference regenerated by the build step alongside the man pages; the epic AC requires only the man page, so `docs/cli.md` is deferred. `--help` is the user manual for now. Owner: docs-step follow-up story.
- **JSON output schema-stability contract** [`crates/orgsidian-cli/src/main.rs`, `crates/orgsidian-parser/src/semantic/`] [MED] — `orgsidian parse --json` emits the current semantic `Document`, serde-derived with camelCase keys, explicitly documented (command `long_about`) as NOT yet a stability promise. Decide the contract when the IPC/specta surface serializes the same types so the CLI and GUI wire formats cannot diverge. Owner: Epic 4+ IPC story.
- **Release-pipeline man-page bundling** [`crates/orgsidian-cli/man/`] [LOW] — The committed `man/*.1` pages are generated at build time per LD-27; packaging them into release artifacts (and install paths per platform) is the release pipeline's job. Owner: Epic 6 packaging.
- **Exit-code contract documentation** [`crates/orgsidian-cli/src/main.rs`] [NIT] — Current posture: 0 success, 1 I/O / defensive-analyze / serialization failure (`ExitCode::FAILURE`), 2 clap usage error. Asserted by tests but not yet documented as a user-facing contract (man page / future `docs/cli.md`). Owner: docs-step follow-up, with `docs/cli.md`.
- **`ClockEntry::duration` serializes as whole seconds** [`crates/orgsidian-parser/src/semantic/drawer.rs`] [NIT] — chrono's `serde` feature covers the date/time types but NOT `TimeDelta`, so Story 2.8 adds a feature-gated `serialize_with` helper emitting `num_seconds()`. Exact for org `=> H:MM` durations (no sub-second component). Revisit the representation (ISO-8601 duration string?) when the JSON schema-stability contract is decided. Owner: Epic 4+ IPC story (with the item above).
- **Pre-existing rustdoc warning in `orgsidian-core` settings** [`crates/orgsidian-core/src/settings/error.rs:6`] [NIT] — (found by Story 2.8's `cargo doc -p orgsidian-core` gate; introduced by Story 1.18) the module doc links `[\`crate::error\`]`, a private module, so rustdoc warns `public documentation … links to private item`. Out of Story 2.8's scope fence (only core `lib.rs`/`Cargo.toml` are touchable). Fix: link `[\`crate::OrgError\`]` instead. Owner: next story that touches `orgsidian-core` source.
- **CLI renderer recursion depth is unbounded** [`crates/orgsidian-cli/src/render.rs`] [NIT] — (review finding 2026-06-10) `render_headline` recurses once per nesting level, same posture as the parser/serializer walks (story-2.4 recursion NIT above). Real vaults are depth-bounded (LD-42); revisit together with the parser-side item if untrusted-input hardening becomes a requirement. Owner: same as the story-2.4 recursion item.
## Deferred from: code review of story-2.5 (2026-06-11)

- **CI build/test step for `tools/corpus-extractor`** [`.github/workflows/pr.yml`] [MED] — The extractor builds/tests only via `--manifest-path` invocations today (workspace `exclude` keeps it out of `cargo test --workspace`); CI never compiles it. Mirror the issues-sync precedent. Owner: Story 2.6's `pr.yml` edit or a CI-hardening pass. _(2026-06-11, Story 2.6: CLOSED — `cargo test (tools/corpus-extractor)` step added to pr.yml adjacent to Step 9.1, plus `rust-cache` `workspaces` registration of both tool crates so the parser path-dep doesn't recompile cold every PR.)_
- **Supply-chain scanning of the extractor's standalone lockfile** [`tools/corpus-extractor/Cargo.lock`] [LOW] — Outside `cargo deny`'s root scope (workspace-excluded); ureq/sha2/clap etc. are unscanned. Add a scoped `cargo deny --manifest-path` (or audit) invocation when the extractor gets a CI step. Owner: same CI-hardening pass as above (ADR 0001 §6). _(2026-06-11, Story 2.6: advisory half CLOSED via the sanctioned "(or audit)" branch — pr.yml Step 11 now also runs `cargo audit --deny warnings $IGNORES --file tools/corpus-extractor/Cargo.lock` with the shared ignore list; green at zero new advisories. License half re-deferred — see the story-2.6 stanza below.)_
- **L2-subset designation within the corpus** [`fixtures/full-nightly.json`] [LOW] — Story 2.7 picks which corpus files feed the Emacs oracle; the manifest schema already carries per-entry constructs/provenance to support the pick. Owner: Story 2.7. _(2026-06-11, Story 2.7: CLOSED — 17-file seed designated from the manifest's constructs/provenance (≥1 per construct kind × 15 + 2 structure-only), committed as `crates/orgsidian-parser/tests/canonical_ast/*.json` with selection rule + schema in its README; concordance pre-flight green on both legs, no KNOWN_DIVERGENCES entries needed.)_
- **git-LFS migration of the vault corpus** [`tests/fixtures/vault-corpus/`, `.gitattributes`] [MED] — git-lfs was unavailable on the generating machine, so the ~2.3 MB corpus (639 .org files) is committed as raw git objects (AC6 pre-authorized fallback); the LFS stanza in `.gitattributes` is commented out with a `FOLLOWUP(Story-2.6)` marker and a scoped `-text` rule keeps EOL bytes intact. Run `git lfs install` + `git lfs migrate import --include="tests/fixtures/vault-corpus/**/*.org"` (history rewrite — coordinate before any corpus growth). Owner: Story 2.6 or the next contributor with git-lfs. _(2026-06-11, Story 2.6 checked: git-lfs still unavailable on this machine (`git lfs version` → "not a git command") AND `git lfs migrate import` rewrites history, which is forbidden while the stacked 2.3→2.6 branches/PRs are unmerged. RE-DEFERRED — new owner: the first maintainer machine WITH git-lfs, after the Epic-2 story stack merges. The `.gitattributes` marker is now `FOLLOWUP(LFS-migration)` with the no-history-rewrite caveat and the non-rewriting `git add --renormalize` alternative documented. See also the story-2.6 stanza below.)_
- **org-mode pin-bump cadence** [`tools/corpus-extractor/src/fetch.rs`] [LOW] — `ORG_RELEASE_TAG`/`SOURCE_SHA256` need a documented bump rhythm; pair with the LD-48 parser-owner submodule review so corpus and grammar track upstream together. Owner: parser owner (CONTRIBUTING §8).

## Deferred from: code review of story-2.6 (2026-06-11)

- **git-LFS migration of the vault corpus (re-deferral, AC6)** [`tests/fixtures/vault-corpus/`, `.gitattributes`] [MED] — git-lfs unavailable on this machine and `git lfs migrate import` rewrites history (forbidden mid-stack with open PRs). Owner: first maintainer machine with git-lfs, AFTER the Epic-2 story stack (2.3→2.6) merges. Non-rewriting alternative documented in the `.gitattributes` `FOLLOWUP(LFS-migration)` marker: uncomment the LFS stanza + `git lfs install` + `git add --renormalize tests/fixtures/vault-corpus` in a normal commit (old raw blobs stay in history, ~2.3 MB, acceptable). _(2026-06-11, Story 2.7 consumer note: `cargo test --workspace` now reads the corpus from disk via `round_trip_full` + `l2_canonical` — post-migration, EVERY CI checkout that runs cargo tests needs `lfs: true`, not just the nightly jobs; both tests already detect pointer stubs and name the remedy.)_
- **License-scan of the extractor's standalone lockfile** [`tools/corpus-extractor/Cargo.lock`, `deny.toml`] [LOW] — AC4's audit-only closure landed (advisories scanned per-PR via `cargo audit --file`); the license half stays open because a scoped `cargo deny --manifest-path tools/corpus-extractor/Cargo.toml check all` FAILS against the root config (bans FAILED, licenses FAILED — the ureq vendored-TLS stack carries licenses outside the root allowlist) and editing `deny.toml`/allowlists was out of Story 2.6 scope by AC4's pre-made decision. Owner: a future supply-chain-hardening pass that's allowed to touch the allowlists.
- **First-CI-run step-timing confirmation (process)** [`.github/workflows/pr.yml`] [LOW] — The <60s epic AC is enforced by `timeout-minutes: 1` + measured local evidence (1.66s test body, debug profile), but actual GitHub-runner numbers are observable only on the first Actions run. The merger must confirm the `L0 round-trip subset gate` step duration on BOTH matrix cells (macos-14 + ubuntu-24.04) in the first run; note that the extractor step pays a one-time cold build (~2-4 min/cell) until the extended rust-cache warms. Owner: PR merger, first Actions run. _(2026-06-11, PR #144 merger: CONFIRMED — first Actions run (27347343476): `L0 round-trip subset gate` 18s on macos-14, 14s on ubuntu-24.04, both well under the 60s AC; `cargo test (tools/corpus-extractor)` 29s/25s, no cold-build penalty observed. CLOSED.)_
- **`dtolnay/rust-toolchain@stable` floating-ref hardening** [`.github/workflows/pr.yml`] [LOW] — pre-existing, already tracked in the story-1.16 stanza above; explicitly NOT fixed in Story 2.6 (out of scope). Owner: workflow-hardening pass.
- **PR body `Closes #22` line + issue #22 label flip (process, review gate)** [LOW] — no PR exists yet for `story/2.6-l0-round-trip-ci-gate` (stacked pre-PR review; the review run is forbidden from creating/editing PRs and issues). The project review customization gates approval on the PR body carrying `Closes #22`: the pipeline step that opens the PR MUST include it, and the `status:review` → `status:done` label flip on issue #22 belongs to the same orchestrator step. Owner: pipeline PR-opening step.

## Deferred from: code review of story-2.7 (2026-06-11)

- **L2 projection-schema deepening beyond the Story-2.3 surface** [`scripts/l2-oracle/projection.el`, `crates/orgsidian-parser/tests/l2_canonical.rs`] [LOW] — schema v1 is the honest intersection today (headline tree + todo + tags + planning); properties, drawers, and body elements join the projection only when Orgsidian's semantic layer models them (KNOWN_DIVERGENCES 1–7 are the gap list). Bump to `l2-projection-v2` + regenerate + review when it happens. Owner: the semantic-layer story that closes each gap.
- **L2 seed expansion cadence** [`scripts/l2-oracle/seed-list.txt`] [LOW] — the 17-file seed covers all 15 manifest construct kinds at release_9.8.5; new construct kinds or richer corpus harvests should grow the seed. Pair with the CONTRIBUTING §8 pin-bump rhythm (corpus + grammar + seed track upstream together). Owner: parser owner (CONTRIBUTING §8).
- **silex-image digest-pinning hardening (R-028)** [`.github/workflows/nightly.yml`] [LOW] — exact-version tags (`silex/emacs:29.4`/`30.2`) rebuild on a rolling basis; the job logs digests at runtime for the audit trail, but a digest-pin (`image@sha256:…`) would freeze the environment fully at the cost of manual bumps for base-image security fixes. Revisit if a rolling rebuild ever changes oracle output (the FAIL/WARN classes would surface it). Owner: workflow-hardening pass.
- **Corpus growth back toward the ~2000-assertion target** [`tools/corpus-extractor/`, `fixtures/full-nightly.json`] [LOW] — observed harvest at release_9.8.5 is 569 entries vs the epic/LD-32 "~2000" aspiration; growth rides future upstream org-mode pin bumps (extraction-rule improvements welcome). `round_trip_full` asserts the 425 floor, not an exact count, precisely so growth lands without test edits. Owner: pin-bump owner (CONTRIBUTING §8).
- **First-nightly-run confirmation (process)** [`.github/workflows/nightly.yml`] [LOW] — the Emacs-29.4 leg, docker-pull timing vs the 20-minute job budget, and the L2 job's first triage output are confirmable only on the first nightly (or `workflow_dispatch`) run; the canonical set was generated and verified locally on Emacs 30.2/Org 9.7.11 only. The merger should dispatch the workflow once after merge and eyeball the `l2-emacs-oracle` job log (digests + 17 OK). Owner: PR merger, first nightly run.
- **PR body `Closes #23` line + issue #23 label flip (process, review gate)** [LOW] — no PR exists yet for `story/2.7-nightly-full-corpus-l2-oracle` (stacked pre-PR review; the review run is forbidden from creating/editing PRs and issues). The project review customization gates approval on the PR body carrying `Closes #23`: the pipeline step that opens the PR MUST include it, and the `status:review` → `status:done` label flip on issue #23 belongs to the same orchestrator step. Owner: pipeline PR-opening step.
