# Story 2.8: Ship `orgsidian parse <file>` CLI command as early public artifact

Status: ready-for-dev

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Metadata

github_issue: 24

## Story

As an **early adopter curious about Orgsidian's parser**,
I want `orgsidian parse <file> [--json]` printing the parsed AST,
So that I can test Orgsidian's fidelity on my own `.org` files before any GUI ships (light-touch John P1 — early public signal at month ~4 instead of month 6).

**Traces:** LD-27 (CLI command tree — first command of the tree), FR-1/FR-2 (exercised, not owned — trace headers stay with `orgsidian-parser`), architecture "CLI Documentation Strategy" (clap annotations as primary docs + `clap_mangen` man pages), architecture Crate Dependency Graph + deny.toml LEAF rule (CLI consumes `orgsidian-core` only).

## Scope Fence (read first)

This is the **first real CLI command** story: `orgsidian parse <file> [--json]`, the façade wiring it needs, its tests, and its man page. It is **not**:

- **NOT the rest of the LD-27 command tree.** No `index`, `query`, `validate-plugin`, `vault` subcommands — only `parse`. Structure the clap derive so future subcommands slot in (a `Command` enum with one variant today), but do not stub unimplemented subcommands.
- **NOT a parser change.** `parse()`, `analyze()`, `serialize()`/`serialize_document()` behavior is frozen. The ONLY permitted `orgsidian-parser` edits are additive serde `Serialize` derives behind a new opt-in `serde` cargo feature (AC2) + the feature plumbing in its `Cargo.toml`. Sentinels stay byte-untouched: `tests/anchor.rs`, `tests/grammar.rs`, `tests/semantic.rs`, `tests/round_trip.rs`, `tests/fixtures/**`, `build.rs`, `src/grammar/`, `grammar/` (LD-48), `src/serializer.rs`.
- **NOT Story 2.5/2.6/2.7 turf — a concurrent pipeline is implementing Story 2.5 right now.** Do not touch `tools/`, root `fixtures/`, `.github/workflows/*`, git-LFS config, or `docs/adr/`. The CLI test fixture is **co-located** at `crates/orgsidian-cli/tests/fixtures/` per the CONTRIBUTING fixture-placement rule (root `fixtures/` is only for ≥2-crate consumers — and it is 2.5's deliverable).
- **NOT `docs/cli.md`.** The architecture's CLI Documentation Strategy names a Markdown reference regenerated by the build step; the epic AC requires only the man page. Defer `docs/cli.md` (record in deferred-work) — `--help` is the user manual for now.
- **NOT stdout-format stability contracts.** Human-readable output is best-effort presentation; the JSON shape is "current semantic `Document`, serde-derived" with NO schema-stability promise yet (the IPC/specta camelCase surface in Epic 4+ is the eventual contract). Document this in the command's `long_about`.

The deliverable is exactly: workspace + `orgsidian-core` façade wiring (AC1), feature-gated `Serialize` on the semantic types (AC2), the `parse` subcommand with human-readable and `--json` output (AC3/AC4), build-time man page (AC5), `assert_cmd` integration tests (AC6), green gates (AC7), deferred-work hygiene (AC8).

## Acceptance Criteria

### AC1 — Dependency wiring respects the LEAF graph rule; `orgsidian-core` grows the parser façade.

**Given** deny.toml's LD-37 LEAF rule (`[[bans.features.deny]]`-style `wrappers = ["orgsidian-core"]` entries at deny.toml:162-196) makes a direct `orgsidian-cli → orgsidian-parser` edge a **hard `cargo deny check bans` failure**, and `orgsidian-core` today has NO parser dependency (verified at story-creation: core deps = thiserror/serde/specta/tracing/orgsidian-vault/toml/dirs),
**When** the wiring lands,
**Then**:

- Root `Cargo.toml` `[workspace.dependencies]` gains `orgsidian-parser = { path = "crates/orgsidian-parser", version = "0.0.0", features = ["serde"] }` — mirror the existing `orgsidian-vault` entry verbatim (explicit `version` + `path` keeps `cargo deny check bans` happy; comment pattern at Cargo.toml:59-68) — plus pins for `clap` (derive feature), `clap_mangen`, and `assert_cmd` (see AC7 for versions).
- `crates/orgsidian-core/Cargo.toml` gains `orgsidian-parser = { workspace = true }` with a Story-2.8 comment (the second cross-crate LEAF edge, same pattern as the Story 1.18 vault edge at core Cargo.toml:34-37).
- `crates/orgsidian-core/src/lib.rs` re-exports the leaf as a façade module: `pub use orgsidian_parser as parser;` (+ one `///` doc line citing the Crate Dependency Graph "Façade" role). `lib.rs` stays re-export-only — no wrapper logic, no new types. Downstream naming: `orgsidian_core::parser::{analyze, serialize_document}`, `orgsidian_core::parser::semantic::Document`.
- `crates/orgsidian-cli/Cargo.toml` gains `[dependencies] orgsidian-core = { workspace = true }`, `clap = { workspace = true }`, `serde_json = { workspace = true }` — and **no `orgsidian-parser` entry, ever**.
- `cargo deny check bans` passes (proves the edge direction is right).

### AC2 — Semantic types are `Serialize` behind an opt-in `serde` feature on `orgsidian-parser`.

**Given** no semantic type derives `Serialize` today (verified: `grep -rn "derive(" src/semantic/` shows Debug/Clone/PartialEq/Eq only) and `Timestamp` carries `chrono::NaiveDate`/`NaiveTime` (chrono workspace pin is `default-features = false, features = ["std"]` — no serde impls active),
**When** the feature is added,
**Then**:

- `crates/orgsidian-parser/Cargo.toml` gains `[features] serde = ["dep:serde", "chrono/serde"]` with `serde = { workspace = true, optional = true }`. Production default stays serde-free (leaf hygiene); `orgsidian-core` activates it via the workspace entry (AC1).
- All public semantic output types derive `Serialize` (NOT `Deserialize` — mirror the `OrgError` Serialize-only precedent, deferred-work story-1.4 item) under `#[cfg_attr(feature = "serde", derive(serde::Serialize))]`: `Document`, `Preamble`, `Directive`, `Headline`, `Tag`, `TodoState`, `TodoConfig`, `TodoSequence`, `Drawer`, `DrawerKind`, `ClockEntry`, `Timestamp`, `Repeater`, `RepeaterKind`, `Delay`, `DelayKind`, `TimeUnit`, `Link`, `LinkKind`. `ParseTree`/`ParseError` are NOT serialized (not output data).
- Structs/enums carry `#[cfg_attr(feature = "serde", serde(rename_all = "camelCase"))]` — matches the project-wide JSON camelCase wire convention (architecture IPC boundary: "Wire format: JSON, camelCase"). `Range<usize>` spans serialize natively as `{"start": …, "end": …}`; chrono Naive types as ISO-8601 strings via `chrono/serde`.
- No behavior change with the feature off: `cargo build -p orgsidian-parser --locked` (no features) compiles bit-for-bit-equivalent code paths; parser test suite (77 tests) untouched and green.
- New derives carry a brief Story-2.8 comment at the feature declaration (not per-type noise).

### AC3 — `orgsidian parse <file>` prints a human-readable AST tree to stdout via clap derive.

**Given** `crates/orgsidian-cli/` is today a stub (`src/main.rs` = one `println!`; `[[bin]] name = "orgsidian"` already declared),
**When** the command is implemented,
**Then**:

- Clap derive definitions live in `crates/orgsidian-cli/src/cli.rs` — **self-contained: `use` only `clap` and `std` items** (this file is `include!`-shared with `build.rs`, AC5). Shape: `pub struct Cli { #[command(subcommand)] pub command: Command }`, `pub enum Command { Parse { file: PathBuf, #[arg(long)] json: bool } }` with `#[command(about, long_about)]` on every level — clap annotations are the **primary documentation** (architecture CLI Documentation Strategy; `--help` is the user manual; `long_about` notes the JSON shape is not yet a stability contract).
- `src/main.rs` stays thin: `Cli::parse()` → read file (`std::fs::read_to_string`) → `orgsidian_core::parser::analyze(&source)` → render → `std::process::ExitCode`. Module doc cites LD-27. **No `Implements FR-` header** — FR-1/FR-2 trace owners are `orgsidian-parser` modules (single-trace-owner rule, CONTRIBUTING §4).
- Human-readable rendering (CLI-side presentation module, e.g. `src/render.rs` — presentation never lives in the parser): an indented tree of headlines showing level (stars), TODO state, title, tags, SCHEDULED/DEADLINE/CLOSED (the `Timestamp::raw`-ish display can use the chrono fields or the timestamp span — dev's choice, it is presentation), and per-headline counts or listings of drawers/links/clocks; preamble summarized first when present. Exact format is dev's choice but MUST be deterministic: **never iterate `Headline::properties` (HashMap) unsorted into output** — sort keys if printed.
- Error posture: unreadable file → message on **stderr** + non-zero exit (distinct from clap's own usage-error exit 2); `analyze()`'s defensive `Err` → same. No `unwrap()`/`expect()`/`panic!` in committed CLI code (house rule; build.rs may `expect`). Empty file / zero headlines renders gracefully (e.g. "(no headlines)"), exit 0.

### AC4 — `orgsidian parse <file> --json` prints the AST as JSON via `serde_json`.

**Given** AC2 + AC3,
**When** `--json` is passed,
**Then**:

- stdout is `serde_json::to_string_pretty(&document)` of the semantic `Document` (camelCase keys per AC2) + trailing newline; nothing else on stdout (scripting-clean: `orgsidian parse f.org --json | jq .` works).
- Serialization failure (theoretically unreachable for these types) maps to the same stderr + non-zero-exit posture — no panic path.

### AC5 — `clap_mangen` generates `crates/orgsidian-cli/man/orgsidian-parse.1` at build time per LD-27.

**Given** the architecture mandates build-time man generation into the in-tree `man/` dir (architecture.md:922 `man/ # clap_mangen-generated man pages (via build.rs)`; :1086 "build.rs invokes clap_mangen"),
**When** `crates/orgsidian-cli/build.rs` is written,
**Then**:

- `[build-dependencies] clap = { workspace = true }, clap_mangen = { workspace = true }`. `build.rs` does `include!("src/cli.rs")`, builds the command via `<Cli as clap::CommandFactory>::command()`, and calls **`clap_mangen::generate_to(cmd, man_dir)`** (exists in clap_mangen 0.3.0; generates the root page + one page per subcommand with hyphenated names → `orgsidian.1` + `orgsidian-parse.1`). Emit `cargo:rerun-if-changed=src/cli.rs` (and `build.rs` itself).
- **Variance to record (do not edit specs):** writing into the source tree from `build.rs` is non-idiomatic Cargo (OUT_DIR is the convention) but architecture/epic-mandated here. Resolve `man/` via `CARGO_MANIFEST_DIR` (NOT relative cwd), create the dir if missing. The generated `man/*.1` files are **committed** (reviewable artifact; release pipeline later bundles them per LD-27) — verify regeneration is byte-stable across two consecutive builds before committing.
- Anti-placebo guard (Story 1.9 discipline): a test in `tests/parse_cmd.rs` asserts `man/orgsidian-parse.1` exists relative to `CARGO_MANIFEST_DIR`, is non-empty, and contains the literal subcommand name — so a future build.rs regression cannot silently stop generating.

### AC6 — `crates/orgsidian-cli/tests/parse_cmd.rs` runs the command via `assert_cmd` against a fixture file.

**Given** the epic-named test file and the architecture testing convention ("CLI integration tests via assert_cmd: `crates/orgsidian-cli/tests/<topic>.rs`", architecture.md:729),
**When** the tests are written,
**Then**:

- `[dev-dependencies] assert_cmd = { workspace = true }` (+ `predicates = { workspace = true }` if used — optional; raw output-capture asserts are equally acceptable) + `serde_json = { workspace = true }`.
- Fixture: `crates/orgsidian-cli/tests/fixtures/parse_cmd.org` (NEW, co-located) — small handcrafted file reusing constructs already proven in `crates/orgsidian-parser/tests/semantic.rs` samples (do not invent new syntax): ≥2 nesting levels, a TODO keyword, tags, a SCHEDULED timestamp, a PROPERTIES drawer, a link, plus a `#+TITLE:` preamble line.
- Test cases via `assert_cmd::Command::cargo_bin("orgsidian")` (the binary name, not the crate name): (1) human mode — exit 0 + stdout contains expected title/TODO/tag substrings (loose substring asserts, not a golden file — format is not a contract); (2) `--json` — exit 0 + stdout parses via `serde_json::from_str::<serde_json::Value>` and asserts real structure (headline title, todoState, nested child, camelCase key presence) — never full-document JSON equality (HashMap key order); (3) missing file → non-zero exit + stderr message + empty stdout; (4) man-page guard (AC5); (5) recommended: `--json` on the fixture round-trips — `serialize_document` of the analyzed fixture equals the fixture bytes (cheap FR-2 cross-check through the public façade).
- Each test asserts a real property (anti-placebo); suite runs in seconds (assert_cmd spawns real processes — keep case count single-digit).

### AC7 — Build, test, and supply-chain gates stay green; dependency delta is declared and bounded.

**Given** clap/clap_mangen/assert_cmd are NOT in `Cargo.lock` today (verified — this story's lockfile delta is real new crates, unlike 2.4's single-edge delta),
**When** the gates run,
**Then**:

- New workspace pins, latest stable per [[feedback_version_policy]] (verified on crates.io 2026-06-10): `clap = { version = "4", features = ["derive"] }` (resolves 4.6.1), `clap_mangen = "0.3"` (resolves 0.3.0; requires clap ^4 — compatible), `assert_cmd = "2"` (resolves 2.2.2), optionally `predicates = "3"` (resolves 3.1.4). Each with a Story-2.8 comment in the house style.
- Expected `Cargo.lock` growth: the clap ecosystem (clap, clap_builder, clap_derive, clap_lex, anstream/anstyle family), `clap_mangen` + `roff`, and the assert_cmd family (dev-only). All MIT/Apache-2.0-class licenses. `cargo deny check licenses bans advisories` must come back ok/ok/ok and `cargo audit` must stay at the **18-allowed-warnings baseline** — if ANY new advisory or license finding appears: **STOP and surface a decision-grade question; never edit deny.toml silently.**
- Note the `--locked` bootstrap order: the lockfile legitimately changes in this story, so first build WITHOUT `--locked` to update `Cargo.lock`, then all subsequent gate runs use `--locked`.
- Gates: `cargo build -p orgsidian-cli --locked` (runs build.rs → man pages regenerate byte-stable, `git status` clean on `man/` after rebuild), `cargo test -p orgsidian-cli --locked` green, `cargo test -p orgsidian-parser --locked` = 77 passed (baseline unchanged; feature off in its own test runs), `cargo test --workspace --locked` green (baseline 122 passed / 11 ignored — new CLI tests add on top, no regressions), `cargo clippy --workspace --all-targets --locked` clean, `cargo fmt --check` clean, `cargo doc -p orgsidian-cli -p orgsidian-core -p orgsidian-parser --no-deps` warning-free.
- Sentinel check via `git status`: parser sentinels (Scope Fence list), `tools/`, root `fixtures/`, `.github/workflows/` all untouched.

### AC8 — Deferred-work hygiene.

**Given** the established per-story stanza pattern in [`deferred-work.md`](_bmad-output/implementation-artifacts/deferred-work.md),
**When** this story completes,
**Then** a `## Deferred from: code review of story-2.8 (YYYY-MM-DD)` stanza is pre-seeded at impl time. Known candidates: rest of the LD-27 command tree (`index`/`query`/`validate-plugin`/`vault` — Epics 3+); `docs/cli.md` Markdown regeneration (architecture CLI Documentation Strategy — docs-step follow-up); JSON output schema-stability contract (decide when IPC/specta serializes the same types, Epic 4+); release-pipeline man-page bundling (Epic 6 packaging); exit-code contract documentation.

## Tasks / Subtasks

- [ ] **T1** — Workspace wiring: root `Cargo.toml` `[workspace.dependencies]` += `orgsidian-parser` (path+version+features, vault-entry pattern), `clap`, `clap_mangen`, `assert_cmd` (+ `predicates` if used), each with Story-2.8 comments. (AC1, AC7)
- [ ] **T2** — Parser serde feature: `[features] serde = ["dep:serde", "chrono/serde"]` + optional serde dep in parser `Cargo.toml`; `cfg_attr` Serialize + camelCase derives on the AC2 type list across `src/semantic/*.rs`. Verify feature-off build + 77-test baseline untouched. (AC2)
- [ ] **T3** — Core façade: parser workspace dep in core `Cargo.toml` (Story-2.8 comment) + `pub use orgsidian_parser as parser;` in core `lib.rs`. Run `cargo deny check bans` early — it proves the LEAF-edge direction before any CLI code exists. (AC1)
- [ ] **T4** — RED first: write `crates/orgsidian-cli/tests/parse_cmd.rs` + `tests/fixtures/parse_cmd.org` against the stub binary; watch every case fail for the right reason. (AC6)
- [ ] **T5** — CLI implementation: `src/cli.rs` (self-contained clap derive defs + about/long_about), `src/main.rs` (thin dispatch, ExitCode, LD-27 module doc), `src/render.rs` (deterministic human-readable tree), `--json` via `serde_json::to_string_pretty`. CLI `Cargo.toml` deps per AC1/AC6. (AC3, AC4)
- [ ] **T6** — Man pages: `build.rs` with `include!("src/cli.rs")` + `CommandFactory` + `clap_mangen::generate_to` into `CARGO_MANIFEST_DIR/man/`; rerun-if-changed directives; build twice, verify byte-stable; commit `man/orgsidian.1` + `man/orgsidian-parse.1`. (AC5)
- [ ] **T7** — GREEN: all parse_cmd tests pass, including the man-page guard and the recommended round-trip cross-check. (AC5, AC6)
- [ ] **T8** — Gates per AC7 (mind the `--locked` bootstrap order); record test-count + lockfile deltas in Completion Notes; sentinel `git status` check. (AC7)
- [ ] **T9** — deferred-work.md: pre-seed the story-2.8 stanza (AC8 candidates). (AC8)
- [ ] **T10** — Commit + open PR. Commit title: `feat(cli): ship orgsidian parse CLI command (Story 2.8, closes #24)` — Conventional Commits scope `cli` per CONTRIBUTING §2. **NO** `Co-Authored-By` trailer, **NO** "Generated with Claude Code" footer, no AI-credit lines. PR body: (a) LEAF-rule wiring rationale (workspace entry + core façade, deny-bans-verified), (b) serde feature posture (opt-in, Serialize-only, camelCase, feature-off baseline untouched), (c) man-page build.rs variance (in-tree `man/`, architecture-mandated, byte-stable), (d) dependency delta + deny/audit verdicts, (e) `Closes #24`. (process)

## Dev Notes

### 1. The LEAF graph rule is a hard gate, not a convention (do not relitigate)

`deny.toml:162-196` declares `wrappers = ["orgsidian-core"]` for every leaf including `orgsidian-parser` — `cargo deny check bans` (CI per-PR) **fails** on a direct `orgsidian-cli → orgsidian-parser` edge. The epic AC text ("the CLI command is added to `crates/orgsidian-cli/` via clap derive") is silent about this; the architecture is not (Crate Dependency Graph: "Consumers … consume `orgsidian-core` only — never reach into leaves directly"). Hence AC1's three-manifest wiring. This story creates the **first** `core → parser` edge — the architecture-anticipated wiring order (architecture.md:654: "`orgsidian-cli` first commands … establishes integration-test surface"; :667: "the CLI is the primary integration-test fixture … any new core feature should have a CLI command exercising it").

### 2. Why a cargo feature for serde, and why Serialize-only

- The parser is a leaf consumed (eventually) by everything; baking serde in unconditionally taxes every future consumer build. `serde = ["dep:serde", "chrono/serde"]` keeps the leaf clean and lets `orgsidian-core` opt in once via the workspace entry's `features = ["serde"]` (feature unification does the rest — the CLI inherits serialization through the façade without naming the feature).
- `chrono/serde` is required: the workspace chrono pin is `default-features = false, features = ["std"]` — `NaiveDate`/`NaiveTime` have no serde impls without it. Zero new crates from this (serde already in-tree); the chrono feature flip is Cargo.lock-visible but version-neutral.
- Serialize-only mirrors `OrgError` (Story 1.4) and avoids the `#[serde(tag)]`-asymmetry trap noted in deferred-work (story-1.4 stanza). Nobody deserializes a `Document` from JSON today; do not invent that contract.
- camelCase `rename_all` aligns CLI JSON with the project-wide wire convention (architecture IPC boundary; Story 1.4's "project-wide camelCase rename" precedent) so Epic 4+ IPC output of the same types won't diverge from what early adopters scripted against.

### 3. build.rs ↔ cli.rs sharing pattern (the one clap_mangen trap)

`build.rs` cannot `use orgsidian_cli::…` (the crate isn't built yet). The standard pattern: keep `src/cli.rs` dependency-minimal (clap + std only) and `include!("src/cli.rs")` from `build.rs`, with clap (derive) duplicated under `[build-dependencies]`. `clap_mangen 0.3.0` provides `generate_to(cmd, dir)` which emits the root page plus one hyphenated page per subcommand — exactly the epic-named `orgsidian-parse.1`. Resolve the output dir from `env!("CARGO_MANIFEST_DIR")`-equivalent (`std::env::var("CARGO_MANIFEST_DIR")` in build.rs) — never cwd-relative. `build.rs` may `expect()` (it's a build tool, not library code). Note `clap_mangen 0.3.0` requires clap ^4 — satisfied by the 4.6.1 pin.

### 4. Real API surface you are consuming (verified at story-creation)

- `orgsidian_parser::analyze(source: &str) -> Result<Document, ParseError>` — total in practice (LD-41 lenience: every input analyzes `Ok`; both `ParseError` arms are defensive). `Document { headlines: Vec<Headline>, todo_config: TodoConfig, preamble: Option<Preamble>, span: Range<usize> }`.
- `Headline` (src/semantic/headline.rs:36-99): `level: u8`, `todo_state: Option<TodoState>`, `title: String`, `tags: Vec<Tag>`, `scheduled/deadline/closed: Option<Timestamp>`, `properties: HashMap<String, String>` (**unordered — sort before printing**), `drawers: Vec<Drawer>`, `clocks: Vec<ClockEntry>`, `links: Vec<Link>`, `raw: String`, `span`, `children: Vec<Headline>`.
- `serialize_document(&Document) -> String` — byte-identical to the analyzed source (Story 2.4 tiling invariant) — powers the cheap AC6 round-trip cross-check.
- Malformed org never errors: it surfaces as ERROR-region content gap-absorbed into `raw`/preamble. The CLI therefore has no "parse failed" UX for org content — only I/O errors fail.

### 5. Test-surface contracts that protect other stories

- Parser sentinels byte-untouched (Scope Fence list) — `tests/round_trip.rs` joined the sentinel set after 2.4 (its `round_trip_subset` name is Story 2.6's CI contract).
- Concurrent Story 2.5 pipeline owns `tools/corpus-extractor/`, root `fixtures/`, git-LFS, `docs/adr/` — zero overlap by construction; if you find yourself editing any of those paths, you have left this story's scope.
- `assert_cmd::Command::cargo_bin("orgsidian")` — the **bin name** (`[[bin]] name = "orgsidian"` already in the stub Cargo.toml), not the crate name `orgsidian-cli`.
- Human-output asserts are substring-loose by design: the rendered format is explicitly NOT a contract (Scope Fence). The JSON asserts go through parsed `Value` field access, never string equality (HashMap order).

### 6. Module/docs conventions (established, follow exactly)

- One concern per file, ~400-line rule; `main.rs` thin; rendering in its own module. No `unwrap()`/`expect()`/`println!`-debugging in committed library/binary code (the renderer's intentional `println!`/`writeln!` output path is the product, not debugging; prefer building a `String` and writing once for testability).
- No `Implements FR-` header in the CLI (single-trace-owner: FR-1 → parser `lib.rs`, FR-2 → `serializer.rs`); cite LD-27 in `main.rs` module docs instead.
- `///` docs on all new pub items; `cargo doc … --no-deps` warning-free. Commit scope `cli` (CONTRIBUTING §2 canonical scope).
- tracing: not required here — the CLI's output IS stdout; do not wire a tracing subscriber in this story (LD-35 subscriber wiring is the shell binary's job; keep the CLI silent on stderr except errors).

### 7. Variances (record in Completion Notes; do NOT edit epics.md / architecture.md / prd.md — epics.md is the GitHub-issues sync-source)

1. Epic AC frames the work as confined to `crates/orgsidian-cli/`; the deny.toml LEAF rule forces additional touches in root `Cargo.toml`, `orgsidian-core` (façade), and `orgsidian-parser` (serde feature). Architecture-mandated wiring, not scope creep.
2. LD-27's one-liner says "`orgsidian parse <file>` — parse + emit AST as JSON"; the epic AC + LD-27's own output rule ("human-readable default; `--json` flag for scripting") win: human-readable is the default, JSON behind `--json`.
3. `clap_mangen` writes into the source tree (`man/`) from build.rs — non-idiomatic Cargo (OUT_DIR convention) but mandated by architecture.md:922/:1086 and the epic AC's exact path; pages are committed.
4. `generate_to` also emits the root `orgsidian.1` (not just the epic-named `orgsidian-parse.1`) — commit both; the epic AC names the minimum, not an exclusive list.

### Project Structure Notes

**Alignment with unified project structure:**

- `crates/orgsidian-cli/src/{main,cli,render}.rs`, `build.rs`, `man/`, `tests/parse_cmd.rs`, `tests/fixtures/parse_cmd.org` — NEW/UPDATE; matches architecture rows :215 (`orgsidian-cli/ # bin: headless CLI`), :921-922 (`man/` via build.rs), :729 (assert_cmd test path), LD-27. Current state: stub `main.rs` (1 `println!`), `Cargo.toml` with `[[bin]] name = "orgsidian"` and zero deps. What must be preserved: the bin name and the crate description.
- `crates/orgsidian-core/{Cargo.toml,src/lib.rs}` — UPDATE: dep edge + one-line façade re-export. Current state: lib.rs is re-export-only (error, registry, test_support, settings); preserve everything, append only.
- `crates/orgsidian-parser/{Cargo.toml,src/semantic/*.rs}` — UPDATE: feature + cfg_attr derives only; zero logic edits. `src/lib.rs`, `src/serializer.rs`, `src/grammar/`, tests, fixtures: byte-untouched.
- Root `Cargo.toml` — UPDATE: 4-5 workspace-dependency entries. `Cargo.lock` — UPDATE (new crates; declared in AC7).
- READ-ONLY / MUST NOT CHANGE: parser sentinels (Scope Fence), `grammar/` (LD-48), `.github/workflows/*` (2.6/2.7 turf), `tools/` + root `fixtures/` (2.5 turf, **concurrent pipeline**), `deny.toml` (STOP-and-ask if any check fails).

**Detected conflicts / variances:** see Dev Notes §7. LD-3 monorepo-era path framing remains stale (established 2.2/2.3/2.4 finding — epics.md paths win); architecture.md is archival, do not edit.

### Testing Standards Summary

- CLI integration tests: `crates/orgsidian-cli/tests/parse_cmd.rs` via assert_cmd (epic-named file; architecture :729 convention). RED-first against the stub binary (T4) — the 2.3/2.4 pattern that worked.
- Baselines that must not move: parser 77 passed; workspace 122 passed / 11 ignored (post-2.4 review). New CLI tests are additive.
- Budget: assert_cmd spawns real binaries — keep to single-digit test cases; first `cargo test -p orgsidian-cli` pays the bin build once, then sub-second.
- CI: macOS-arm64 + Ubuntu-LTS per PR, Windows + Arch nightly — no CI-config change in this story. The CLI crate has no Windows skip baggage (the export_bindings Windows skip is shell-app-only).

### Previous Story Intelligence (from Stories 2.3 + 2.4)

- **API stability you can lean on:** `analyze()` total (lenience contract), `Document`/`Headline` owned data (no lifetimes — exactly why the CLI can hold and serialize them), `serialize_document` byte-identity proven by 77 parser tests + proptest. Nothing in this story needs to re-prove parser behavior — exercise it through the façade only.
- **`properties` HashMap caveat** (2.3, restated 2.4): unordered, last-wins — drove AC3's sort-before-print and AC6's no-full-JSON-equality rules.
- **Process patterns that worked:** RED-first; scope-fence discipline (2.4's review had 0 decision-needed findings); variance-recording instead of spec-editing; zero-new-deps-beyond-declared with STOP-and-ask; pre-seeded deferred-work stanza; terse batch fixes in review ([[feedback_batch_fixes_terse]]).
- **Hygiene:** commit scope per crate; `closes #N` in commit title; no AI-credit lines ([[feedback_no_co_author_credit]]); cargo-audit baseline 18 allowed warnings (gtk-rs/Tauri unmaintained set) — count must not move.
- **2.4 deviation precedent:** if the pipeline step forbids PR creation/label flips, commit locally with the prescribed message and record the deviation in Completion Notes — the next pipeline step owns PR + issue-label sync (issue #24 verified to exist, `status:backlog`).

### Git Intelligence Summary

`git log --oneline -5` at story-write: `2f93b5d` Merge PR #139 (2.2) ← `9c3b9c8` 2.2 review fixes ← `6424a05` 2.2 impl… **plus Story 2.3 + 2.4 commits local to this lineage** — branch `story/2.8-parse-cli-command` is **stacked on the completed 2.4 branch** (2.3/2.4 are `done` in sprint-status but possibly not yet merged to origin/main; flag in the PR description if so). Per-story pattern: one story-file commit + one impl commit + one review-fixes commit, PR-merged. `crates/orgsidian-cli/` has been touched only by the Story 1.2 scaffold — no conflicting in-flight work (Story 2.5's concurrent pipeline does not touch the CLI crate).

### Latest Technical Information

- **clap 4.6.1** (workspace pin `{ version = "4", features = ["derive"] }`; verified crates.io 2026-06-10). Derive API: `Parser`, `Subcommand`, `CommandFactory` (for build.rs), `#[command(about, long_about)]`, `#[arg(long)]`. MIT/Apache-2.0.
- **clap_mangen 0.3.0** (pin `"0.3"`; requires clap ^4). API: `Man::new(cmd).render(&mut buf)` per page, or **`generate_to(cmd, out_dir)`** for root + all subcommands (hyphenated filenames) — use `generate_to`. Re-exports `roff ^1`. MIT/Apache-2.0.
- **assert_cmd 2.2.2** (pin `"2"`). API: `Command::cargo_bin("orgsidian")`, `.arg(…)`, `.assert().success()/.failure()`, `.get_output()`. Companion **predicates 3.1.4** (pin `"3"`) optional. MIT/Apache-2.0.
- **serde_json 1.x** already workspace-pinned (`serde_json = "1"`); **serde 1.x** already workspace-pinned with derive. **chrono 0.4.44** in-tree; the `serde` cargo feature adds ISO-8601 impls for the Naive types — feature flip only, no version movement.
- No version bumps anywhere else ([[feedback_version_policy]] — latest-stable pins satisfied; Tauri ecosystem untouched).

### References

- Source story: [`epics.md:873-886`](_bmad-output/planning-artifacts/epics.md#L873-L886) — Story 2.8 user story + 4 ACs ("Given Stories 2.3 + 2.4" — both `done` on this branch). Epic 2 overview: [`epics.md:744-746`](_bmad-output/planning-artifacts/epics.md#L744) ("tweet-ready early signal"), [`epics.md:360`](_bmad-output/planning-artifacts/epics.md#L360).
- LD-27 CLI command tree: [`architecture.md:499-507`](_bmad-output/planning-artifacts/architecture.md#L499) (full tree + "human-readable default; `--json` flag"); CLI as integration-test surface: [`architecture.md:654,667`](_bmad-output/planning-artifacts/architecture.md#L654); assert_cmd convention: [`architecture.md:729`](_bmad-output/planning-artifacts/architecture.md#L729); CLI Documentation Strategy (clap annotations + clap_mangen + docs/cli.md): [`architecture.md:1083-1087`](_bmad-output/planning-artifacts/architecture.md#L1083); `man/` dir: [`architecture.md:921-922`](_bmad-output/planning-artifacts/architecture.md#L921); Crate Dependency Graph + consumer rule: [`architecture.md:1013-1028`](_bmad-output/planning-artifacts/architecture.md#L1013); IPC camelCase wire convention: [`architecture.md:1033`](_bmad-output/planning-artifacts/architecture.md#L1033); fixture placement rule: [`architecture.md:1011`](_bmad-output/planning-artifacts/architecture.md#L1011); process boundary (CLI = separate executable): [`architecture.md:1032`](_bmad-output/planning-artifacts/architecture.md#L1032).
- deny.toml LEAF rule: [`deny.toml:162-196`](deny.toml#L162) (`wrappers = ["orgsidian-core"]` per leaf).
- Real code consumed/modified: [`crates/orgsidian-parser/src/lib.rs`](crates/orgsidian-parser/src/lib.rs) (`analyze`/`serialize_document` re-exports, chrono/tree_sitter re-export rationale), [`crates/orgsidian-parser/src/semantic/mod.rs`](crates/orgsidian-parser/src/semantic/mod.rs) (`Document`/`Preamble`/`Directive` + pub-use list at :35-39 = the AC2 derive list), [`crates/orgsidian-parser/src/semantic/headline.rs`](crates/orgsidian-parser/src/semantic/headline.rs) (`Headline` fields :36-99), [`crates/orgsidian-core/src/lib.rs`](crates/orgsidian-core/src/lib.rs) + [`Cargo.toml`](crates/orgsidian-core/Cargo.toml) (vault-edge pattern :34-39), [`crates/orgsidian-cli/Cargo.toml`](crates/orgsidian-cli/Cargo.toml) (stub, `[[bin]] name = "orgsidian"`), root [`Cargo.toml:59-92`](Cargo.toml#L59) (workspace-dep comment patterns; chrono pin :92).
- Previous stories: [`2-4-implement-round-trip-faithful-serializer.md`](_bmad-output/implementation-artifacts/2-4-implement-round-trip-faithful-serializer.md) (serializer contract, conventions §6, gates list, baselines); [`2-3-implement-semantic-layer-todo-cycling-drawer-types-timestamps-link-types.md`](_bmad-output/implementation-artifacts/2-3-implement-semantic-layer-todo-cycling-drawer-types-timestamps-link-types.md) (semantic API design rationale).
- Deferred-work context: [`deferred-work.md`](_bmad-output/implementation-artifacts/deferred-work.md) — story-1.4 `OrgError` Serialize-only precedent; story-2.2/2.3/2.4 parser stanzas (no CLI blockers among them).
- CONTRIBUTING: §2 commit scope `cli`, §4 FR traceability (single trace owner), fixture placement rule.
- GitHub issue: [#24](https://github.com/orgsidian/orgsidian/issues/24) `[Story 2.8] Ship orgsidian parse <file> CLI command as early public artifact` (verified, `status:backlog`).
- [[feedback_version_policy]], [[feedback_no_co_author_credit]], [[feedback_batch_fixes_terse]].

### Project Context Reference

- [`prds/prd-orgsidian-2026-05-19/prd.md`](_bmad-output/planning-artifacts/prds/prd-orgsidian-2026-05-19/prd.md) — FR-1/FR-2 (§4.1), trust-contract principles (§3).
- [`architecture.md`](_bmad-output/planning-artifacts/architecture.md) — LD-27 (CLI tree), LD-35 (logging — subscriber NOT wired here), LD-37 (LEAF graph rule), LD-41 (lenience), LD-48 (grammar READ-ONLY); AI-agent implementation rules.
- [`epics.md`](_bmad-output/planning-artifacts/epics.md) — Epic 2 (Stories 2.1 → 2.8); Story 2.5 (concurrent, disjoint turf).
- [`CONTRIBUTING.md`](CONTRIBUTING.md) — §2 scopes, §4 traceability.
- [`docs/parser/KNOWN_DIVERGENCES.md`](docs/parser/KNOWN_DIVERGENCES.md) — context for what the AST will show early adopters (entry 1 link-scan false positives, entry 8 scanner.c indent bug).
- [`deferred-work.md`](_bmad-output/implementation-artifacts/deferred-work.md) — story-2.2 → 2.4 stanzas.

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List

## Change Log

- 2026-06-10 — Story created (ultimate context engine analysis completed — comprehensive developer guide created; grounded in the real stub CLI crate, the deny.toml LEAF graph rule forcing the core façade + serde-feature design, and crates.io-verified clap 4.6.1 / clap_mangen 0.3.0 / assert_cmd 2.2.2). Status: ready-for-dev.
